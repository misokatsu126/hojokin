-- =============================================================
-- 人間メモ 追加  discovery_note_schema.sql
-- 既存SQLは編集していません。実行済みDBに追加実行してください（冪等）。
--
-- 目的：各候補に担当者がメモを残せるようにする
--   （例「市役所確認済み」「対象外だった」「来年度狙う」）。
-- =============================================================

alter table discovered_items add column if not exists human_note text;
